Step 3 — Logging and adjacent features. First working set completed70×1; later working sets remain60×8, no increase prompt. Add drop still creates D1 using existing20% reduction56kg; incomplete drop stays separate from working-set count. Existing Add drop behavior clears the rest countdown (confirmed in unchanged handler). Native input labels and completion pressed state present in DOM; full screen-reader behavior and iPhone background timers not tested.