Step 6 — Reload and import lifecycle. In a separate empty local origin, imported a synthetic progressed workout via normal Import JSON. Reloaded the app;62.5/50/42.5 starting loads and0/9 completion remained; increase disclosure and undo retained. Enter opens the native summary control. This fixed the earlier React-only notice loss. Different-workout import and undo still to check after capture.
Follow-up: importing a different synthetic active workout (30kg sets, no adjustment metadata), then Resume, shows no increase notice (count0). The prior workout explanation does not leak.